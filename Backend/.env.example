# Database

# Connect to Supabase via connection pooling
DATABASE_URL=
# Direct connection to the database. Used for migrations
DIRECT_URL=


SUPABASE_URL=
SUPABASE_ANON_KEY=
NODE_ENV=development
PORT=3000

# JWT (will be used in Phase 2)
JWT_SECRET=
JWT_EXPIRES_IN=
JWT_REFRESH_SECRET=
JWT_REFRESH_EXPIRES_IN

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=

# Mail
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=pson4282@gmail.com
MAIL_PASS=
MAIL_FROM="TeamZoneVN" <pson4282@gmail.com>

# CORS
# Development: leave as * to allow all origins
# Production: comma-separated list of allowed origins, e.g.:
# CORS_ORIGIN=https://yourapp.com,https://admin.yourapp.com
CORS_ORIGIN=*

# Redis (caching, leaderboard, presence, rate limiting)
REDIS_HOST=
REDIS_PORT=
REDIS_PASSWORD=
